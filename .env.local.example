# ============================================================
# SmartBurger — Variables de Entorno
# Copia este archivo a .env.local y rellena los valores reales
# cp .env.local.example .env.local
# ============================================================

# ------------------------------------------------------------
# SUPABASE (requerido)
# Obtén estos valores en: https://supabase.com/dashboard → Settings → API
# ------------------------------------------------------------

# URL del proyecto (pública — se expone en el navegador)
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co

# Anon/public key (pública — se expone en el navegador)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role key (PRIVADA — solo en servidor, nunca en el cliente)
# Usada en API routes para operaciones administrativas sin RLS
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ------------------------------------------------------------
# GOOGLE GEMINI (requerido para el chat IA)
# Obtén tu API key en: https://aistudio.google.com/app/apikey
# ------------------------------------------------------------

GEMINI_API_KEY=AIzaSy...

# ------------------------------------------------------------
# APP (opcional pero recomendado)
# ------------------------------------------------------------

# URL pública de la app (sin trailing slash)
# Desarrollo: http://localhost:3000
# Producción: https://tu-dominio.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ------------------------------------------------------------
# OPENAI — Agente de Inteligencia del Admin (opcional)
# Obtén tu API key en: https://platform.openai.com/api-keys
# Sin esta variable el agente funciona en modo preview (datos reales, sin GPT)
# Para activar GPT-4o completo:
#   1. Agrega esta clave
#   2. npm install openai
#   3. Descomentar el bloque TODO en app/api/admin/analyze/route.ts
# ------------------------------------------------------------

# OPENAI_API_KEY=sk-...
